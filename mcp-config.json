{
  "mcpServers": {
    "android-driver": {
      "command": "uv",
      "args": ["run", "--project", "${CLAUDE_PLUGIN_ROOT}", "android-driver"],
      "env": {
        "ANDROID_DRIVER_PROJECT": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
